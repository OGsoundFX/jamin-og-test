<div class="container">
  <div id="back-button-display">
    <%= link_to "Back", root_path, class: "glyphicon glyphicon-star" %>
  </div>
</div>

<div class="container">
  <h5 id="profile-title-display">Profile</h5>
</div>

<div class="container card-container">
  <div class="row">
    <div class="user_pic">
      <div class="col-3">
        <%= cl_image_tag @user.photo.key, height: 400, width: 400, crop: :fill, class: "avatar-large" %>
        <div>
          <h4 id="user-name-one-line"><strong><%= @user.first_name %> <%= @user.last_name %></strong></h4>
        </div>
      </div>
    </div>
    <div class="col-8">
      <div class="bio-display">
        <h4>About me</h4>
        <p><%= @user.bio %></p>
        <br>
        <ul class="inline-list d-flex justify-content-between" style="padding: 0;">
          <% @user.user_instruments.first(3).each do |user_instrument| %>
            <li>
              <h6>
                <%= cl_image_tag user_instrument.instrument.photo.key, class: "icon" %> - <%= user_instrument.level %>
                <% user_instrument.instrument.name %>
              </h6>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>

<br>

<!--
<div class="container card-container">
  <h5>Reviews</h5>
</div>

<br>
-->

<div class="container card-container">
  <h5>Past Jam Sessions</h5>
</div>
